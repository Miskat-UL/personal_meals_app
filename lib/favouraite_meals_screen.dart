import 'package:flutter/material.dart';

class FavouraitMealsDcreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Favourait meals"),
      ),
    );
  }
}
